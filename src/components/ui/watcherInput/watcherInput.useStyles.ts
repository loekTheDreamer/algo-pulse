import {StyleSheet} from 'react-native';

export const useStyles = () => {
  return StyleSheet.create({
    inputCard: {
      padding: 16,
      borderRadius: 12,
      marginTop: 16,
    },
  });
};
