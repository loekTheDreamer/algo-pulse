import React from 'react';
import {Icon, IconElement} from '@ui-kitten/components';

export const TrendingIcon = (props: any): IconElement => (
  <Icon {...props} name="trending-up-outline" />
);

export const DownloadIcon = (props: any): IconElement => (
  <Icon {...props} name="cloud-download-outline" />
);

export const CalendarIcon = (props: any): IconElement => (
  <Icon {...props} name="calendar-outline" />
);
