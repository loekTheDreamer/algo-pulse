import React from 'react';
import {Icon, IconElement, IconProps} from '@ui-kitten/components';

export const SendIcon = (props: IconProps): IconElement => (
  <Icon {...props} name="activity" width={16} height={16} />
);
